# 🪩 Vibe Matcher — Mini Fashion Recommendation Prototype

A lightweight recommendation system that matches **fashion products** to a user's **vibe query** (e.g., _"energetic urban chic"_) using **Gemini embeddings** and **cosine similarity**.

---

## 🎯 Project Overview

This prototype demonstrates how **semantic embeddings** can be used to build a vibe-based product recommender.  
It accepts a free-text query describing the desired “vibe” and returns the **top-3 most similar products** from a small mock dataset.

---

## 1️⃣ Prerequisites
- Python 3.10+
- OpenAI API key (you can also use gemini API key with openAI)
- Packages: `openai`, `pandas`, `scikit-learn`, `matplotlib`
